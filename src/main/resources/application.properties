spring.application.name=Support Ticket Management

# Database Configuration
# (Using direct JDBC in TicketDAO, so Spring datasource properties are removed)

# Automatically run schema.sql and data.sql on startup
# (Not applicable since we removed spring datasource, we'll keep it for clarity though or remove it)

# Thymeleaf configuration (disable caching for development)
spring.thymeleaf.cache=false

# Port configuration
server.port=8080
